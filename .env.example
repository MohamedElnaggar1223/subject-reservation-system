# Environment Variables Example
# Copy this file to create your environment files for each app

#==========================================
# API (apps/api/.env)
#==========================================

# Better-auth Secret (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=your_random_secret_here

# Better-auth URL (your API base URL)
BETTER_AUTH_URL=http://localhost:3001

# Database connection string
DATABASE_URL=postgresql://user:password@localhost:5432/myapp

# Node environment
NODE_ENV=development

# API port
PORT=3001

# CORS Origins (comma-separated)
# Development: include localhost and Expo patterns
# Production: only your actual domains
CORS_ORIGINS=http://localhost:3000

# Cookie Domain (optional)
# Development: leave empty or use localhost
# Production: .yourdomain.com (for api.yourdomain.com + app.yourdomain.com)
COOKIE_DOMAIN=

#==========================================
# CLOUDFLARE R2 CONFIGURATION (apps/api/.env)
#==========================================

# R2 Account ID (from Cloudflare dashboard > R2 > Overview)
R2_ACCOUNT_ID=your_account_id_here

# R2 Access Credentials (create in Cloudflare dashboard > R2 > Manage R2 API Tokens)
# Go to: R2 > "Manage R2 API Tokens" > "Create API Token"
# Permissions: Select "Workers R2 Storage" → "Edit"
# You'll get Access Key ID and Secret Access Key (S3-compatible credentials)
R2_ACCESS_KEY_ID=your_access_key_id_here
R2_SECRET_ACCESS_KEY=your_secret_access_key_here

# R2 Bucket Name (create bucket in Cloudflare dashboard)
R2_BUCKET_NAME=your-bucket-name

# IMPORTANT: Keep your bucket PRIVATE (do NOT enable public access)
# Files are accessed securely via temporary signed URLs generated by the API
# This provides better security than public buckets

#==========================================
# WEB (apps/web/.env.local)
#==========================================

# API URL for client-side requests
NEXT_PUBLIC_API_URL=http://localhost:3001

# Node environment
NODE_ENV=development

#==========================================
# MOBILE (apps/app/.env.local)
#==========================================

# API URL for mobile app
# Development: use your machine's IP address (not localhost)
# Example: http://192.168.1.100:3001
EXPO_PUBLIC_API_URL=http://localhost:3001

#==========================================
# PRODUCTION NOTES
#==========================================

# For production deployment:
# 1. Set COOKIE_DOMAIN to your domain (e.g., .example.com)
# 2. Update CORS_ORIGINS to include your production domains only
# 3. Set NODE_ENV=production
# 4. Use strong BETTER_AUTH_SECRET (32+ random characters)
# 5. Use production database URL with SSL
# 6. Update API URLs to production domains
